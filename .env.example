# Visant - Multi-Tenant Camera Monitoring SaaS
# Environment Variables Configuration
# Copy this file to `.env` and fill in your real API keys and secrets.

# =============================================================================
# REQUIRED: Database Configuration
# =============================================================================
# PostgreSQL database connection URL
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/visant

# =============================================================================
# REQUIRED: Supabase Authentication
# =============================================================================
# Supabase project URL (from your Supabase dashboard)
SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous/public key (from your Supabase dashboard)
SUPABASE_KEY=your-supabase-anon-key-here

# Supabase service role key (from your Supabase dashboard - keep secret!)
SUPABASE_SERVICE_KEY=your-supabase-service-role-key-here

# Supabase JWT secret (from your Supabase dashboard - keep secret!)
SUPABASE_JWT_SECRET=your-supabase-jwt-secret-here

# =============================================================================
# REQUIRED: AI Classifier API Keys
# =============================================================================
# OpenAI API key for image classification
OPENAI_API_KEY=your-openai-api-key-here

# Google Gemini API key for image classification
GEMINI_API_KEY=your-gemini-api-key-here

# =============================================================================
# OPTIONAL: Email Notification Settings (SendGrid)
# =============================================================================
# SendGrid API key for sending email alerts
# SENDGRID_API_KEY=your-sendgrid-api-key-here

# Sender email address for alerts
# ALERT_FROM_EMAIL=alerts@example.com

# Environment label for email subject lines (e.g., "prod", "staging")
# ALERT_ENVIRONMENT_LABEL=prod

# =============================================================================
# OPTIONAL: CORS Configuration
# =============================================================================
# Comma-separated list of allowed origins for CORS
# Default: http://localhost:3000,http://localhost:5173 (React/Vite dev servers)
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://yourdomain.com

# =============================================================================
# OPTIONAL: AWS S3 Storage (for image uploads)
# =============================================================================
# AWS credentials for S3 storage
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# S3 bucket name for storing capture images
# AWS_S3_BUCKET=visant-captures

# AWS region for S3 bucket
# AWS_REGION=us-east-1

# Optional: Custom S3-compatible endpoint URL (for MinIO, DigitalOcean Spaces, etc.)
# S3_ENDPOINT_URL=https://your-endpoint.com
# S3_REGION=us-west-2

# =============================================================================
# OPTIONAL: Application Settings
# =============================================================================
# Base URL for the cloud service (used in emails and links)
# OK_CLOUD_BASE_URL=https://app.visant.com

# Enable timing debug mode (overrides config/cloud.json setting)
# ENABLE_TIMING_DEBUG=true
